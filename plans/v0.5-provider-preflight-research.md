# SpanVouch v0.5 Provider Preflight Research

**Checked:** 2026-07-31 (Asia/Shanghai)

**Scope:** Public, first-party Alibaba Cloud Model Studio, DeepSeek, and European Central Bank documentation and data only. No credential value or account data was read.

**Decision:** The user explicitly approved `qwen3.7-plus` for B4/B5 after reviewing this preflight.

## Findings

1. Alibaba Cloud's current Model Studio model catalog redirects to `https://help.aliyun.com/zh/model-studio/models`. The retrieved catalog lists `qwen3.7-flash`, `qwen3.7-plus`, and `qwen3.7-max`. It does not list `qwen3-14b` in the retrieved page. This proves only that `qwen3-14b` is absent from the public catalog snapshot; account-specific availability must still be checked after credentials are configured.
2. The same official catalog gives workspace-specific, regional OpenAI-compatible API roots such as `https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`. Therefore the checked-in hash of `https://dashscope.aliyuncs.com/compatible-mode/v1` cannot authorize the current workspace endpoint.
3. Alibaba Cloud's official OpenAI compatibility documentation uses `DASHSCOPE_API_KEY` and a workspace-specific regional `/compatible-mode/v1` Base URL. SpanVouch may continue to expose the application-specific name `SPANVOUCH_QWEN_API_KEY`, but the runtime value and normalized URL hash must be bound to the selected workspace before a paid manifest is approved.
4. Alibaba Cloud's official pricing page at `https://help.aliyun.com/zh/model-studio/model-pricing` lists China-region `qwen3.7-plus` prices for requests up to 256K input tokens. The list prices are CNY 2.00 per million input tokens and CNY 8.00 per million non-thinking output tokens. A temporary 20-percent discount is advertised, but SpanVouch freezes the higher list prices so budget safety does not depend on a promotion.
5. DeepSeek's official pricing page lists `deepseek-v4-flash` at USD 0.14 per million cache-miss input tokens and USD 0.28 per million output tokens. SpanVouch uses the cache-miss input rate for every request, even when a provider-side cache might make the actual charge lower.
6. The European Central Bank observations for 2026-07-30 are CNY 7.7521 per EUR and USD 1.1476 per EUR. Their cross-rate is CNY 6.7550540257929592192401533635 per USD. SpanVouch adds a 5-percent buffer and rounds upward to CNY 0.10, freezing a conservative reserve rate of CNY 7.10 per USD.
7. The canonical files are checked in as `evals/configs/phase5-deepseek-v4-flash-pricing.json` and `evals/configs/phase5-qwen3.7-plus-pricing.json`. Their exact hashes and public provenance are frozen in `phase5-pilot.json`. Existing fail-closed composition rejects noncanonical bytes or any hash, provider, model, source, date, or currency drift.

## Recommendation

Use `qwen3.7-plus` for B4/B5 if it is available in the user's Model Studio workspace. It is the balanced current Qwen tier between `flash` and `max`, which fits a verifier study better than optimizing only for lowest cost or maximum model capability. The material preregistration change from `qwen3-14b` has now received explicit approval.

Alternative choices:

- `qwen3.7-max`: strongest current listed Qwen tier, with higher expected experiment cost and a larger capability gap from the generator.
- `qwen3.7-flash`: lowest-latency/cost-oriented listed tier, with greater risk that verifier weakness dominates the comparison.

## Required Inputs Before Paid Preflight

- explicit approval of the exact Qwen model identifier;
- Alibaba Cloud Model Studio workspace region and OpenAI-compatible Base URL;
- Qwen API key supplied only through local environment configuration;
- DeepSeek API key supplied only through local environment configuration;
- an absolute persistent Phase 5 budget-ledger path;
- a fresh pilot configuration hash and manifest authorization after all provenance values are frozen.

## Primary Sources

- Alibaba Cloud Model Studio model catalog: https://help.aliyun.com/zh/model-studio/models
- Alibaba Cloud Model Studio pricing: https://help.aliyun.com/zh/model-studio/model-pricing
- Alibaba Cloud Model Studio OpenAI compatibility: https://www.alibabacloud.com/help/en/model-studio/compatibility-of-openai-with-dashscope
- DeepSeek models and pricing: https://api-docs.deepseek.com/quick_start/pricing/
- ECB CNY/EUR reference series: https://data-api.ecb.europa.eu/service/data/EXR/D.CNY.EUR.SP00.A?startPeriod=2026-07-30&endPeriod=2026-07-30&format=csvdata
- ECB USD/EUR reference series: https://data-api.ecb.europa.eu/service/data/EXR/D.USD.EUR.SP00.A?startPeriod=2026-07-30&endPeriod=2026-07-30&format=csvdata
